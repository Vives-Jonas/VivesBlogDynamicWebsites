@model BlogPost


<div class="card">
    <div class="card-body">
        <h5 class="card-title">@Model.Title</h5>
        @if (Model.Author is not null) 
        {
            <h6 class="card-subtitle mb-2 text-muted">@Model.Author.Name</h6>
        }        
        <p class="card-text">@Model.Content</p>
        @if ((Boolean)ViewData["IsDetail"])
        {
            <a asp-area="" asp-controller="Blog" asp-action="Index" class="card-link">Go Back</a>
        }
        else
        {
            <a asp-area="" asp-controller="Blog" asp-action="Detail" asp-route-id="@Model.Id" class="card-link">Details</a>
        }
                
    </div>
</div>
